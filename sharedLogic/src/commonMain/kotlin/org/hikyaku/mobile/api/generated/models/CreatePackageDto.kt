/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package org.hikyaku.mobile.api.generated.models

import org.hikyaku.mobile.api.generated.models.PackageDimensionsDto

import kotlinx.serialization.*
import kotlinx.serialization.descriptors.*
import kotlinx.serialization.encoding.*

/**
 * 
 *
 * @param warehouseId warehouse.id the package is dispatched from. Must belong to the active organisation.
 * @param fromCustomerId customer.id of the sender.
 * @param toCustomerId customer.id of the recipient. Its customer_location is the routed stop; a recipient with no geocode cannot be assigned.
 * @param dimensions 
 * @param id Client-supplied packages.id. Both clients mint a UUID before the call so they can name the storage path for photos; supplying it here keeps that working and makes the create idempotent on replay. Omitted, the server generates one.
 * @param trackingNumber Human-facing tracking number. Omitted, the packages_set_tracking_number trigger generates one. Re-sending an existing number with an identical payload replays the original package instead of creating a second.
 * @param deliveryNotes Free-text notes for the driver.
 * @param deadlineAt Hard deadline — the promise made to the customer, stored in package_delivery_window.scheduled_arrival and never overwritten by the planner. Packages WITHOUT a deadline are the ones eligible to be bumped off a shift to make room for one that has a deadline.
 * @param autoAssign Run assignment immediately after creation. The mobile create-shift wizard MUST send false: it creates packages then hands their ids to POST /api/v1/optimisation/adhoc, which rejects a package that already belongs to an optimisation.
 */
@Serializable

data class CreatePackageDto (

    /* warehouse.id the package is dispatched from. Must belong to the active organisation. */
    @SerialName(value = "warehouseId") @Required val warehouseId: kotlin.String,

    /* customer.id of the sender. */
    @SerialName(value = "fromCustomerId") @Required val fromCustomerId: kotlin.String,

    /* customer.id of the recipient. Its customer_location is the routed stop; a recipient with no geocode cannot be assigned. */
    @SerialName(value = "toCustomerId") @Required val toCustomerId: kotlin.String,

    @SerialName(value = "dimensions") @Required val dimensions: PackageDimensionsDto,

    /* Client-supplied packages.id. Both clients mint a UUID before the call so they can name the storage path for photos; supplying it here keeps that working and makes the create idempotent on replay. Omitted, the server generates one. */
    @SerialName(value = "id") val id: kotlin.String? = null,

    /* Human-facing tracking number. Omitted, the packages_set_tracking_number trigger generates one. Re-sending an existing number with an identical payload replays the original package instead of creating a second. */
    @SerialName(value = "trackingNumber") val trackingNumber: kotlin.String? = null,

    /* Free-text notes for the driver. */
    @SerialName(value = "deliveryNotes") val deliveryNotes: kotlin.String? = null,

    /* Hard deadline — the promise made to the customer, stored in package_delivery_window.scheduled_arrival and never overwritten by the planner. Packages WITHOUT a deadline are the ones eligible to be bumped off a shift to make room for one that has a deadline. */
    @SerialName(value = "deadlineAt") val deadlineAt: kotlinx.datetime.Instant? = null,

    /* Run assignment immediately after creation. The mobile create-shift wizard MUST send false: it creates packages then hands their ids to POST /api/v1/optimisation/adhoc, which rejects a package that already belongs to an optimisation. */
    @SerialName(value = "autoAssign") val autoAssign: kotlin.Boolean? = true

) {


}

